"use client";
/*
interface Employee {
  name: string;
  toString(): string;
  serialize(): string;
  // Här kan metoden namnges mer utförligt för att förklara vad den gör, den bryter alltså mot principen att man ska "Use meaningful and pronounceable variable names"
}

export default function About() {
  class EmployeeImpl implements Employee {
    name: string;

    constructor(name: string) {
      this.name = name;
    }

    toString() {
      return `${this.name}`;
    }

    // klassen har två funktioner vilket engligt principen att "Functions Should Do One Thing" bör undvikas. 

    // 
    serialize() {
      return this.toString();
    }
  }

  const employee: Employee = new EmployeeImpl("John Doe");

  return (
    <main className="flex justify-center">
      <h2>Spagetti Two</h2>
      <p>Name: {employee.name}</p>
    </main>
  );
}
 */

export default function MyButton() {
  return <button> press me dawd </button>;
}
